# 2026-07-17

## CLI 与模板解耦

- 将 `packages/create-chrome-ext-vue/templates/` 独立为 `packages/template-vue3/`（包名 `create-chrome-ext-vue-template`）
- CLI 通过 `resolveTemplateDir()` 三级优先解析模板路径：`--template` 参数 → npm 依赖 → monorepo 本地回退
- CLI 新增 `--template` 参数支持自定义模板路径
- `.gitignore` → `_gitignore`（npm 发布时 `.gitignore` 被排除，采用 `_gitignore` 占位，`copyDir` 时自动重命名）
- 模板包添加 `.npmignore` 确保发布内容正确
- CLI package.json：新增 `create-chrome-ext-vue-template` 依赖，`files` 移除 `templates`，版本升至 1.1.0
- 根 tsconfig.json：移除废弃的 `@chrome-ext-vue/core` 路径映射
- 根 package.json 添加发布脚本：`publish:template`、`publish:cli`、`publish:all`、`pack:template`、`pack:cli`
- 发布顺序：先 template 后 cli（cli 依赖 template）
- `npm run build` + `npm pack --dry-run` 验证通过
